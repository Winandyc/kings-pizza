import React from 'react';

import './styles.css';

const News = () => {
    return (
        <section className="news-section">
            <h2 className="news-title">Pizza du moment</h2>
        </section>
    );
};

export default News;
